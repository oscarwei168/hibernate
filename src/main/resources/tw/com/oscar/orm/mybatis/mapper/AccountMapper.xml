<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tw.com.oscar.orm.mybatis.mapper.AccountMapper">
    <select id="selectAccount" parameterType="long" resultType="Account">
        SELECT * FROM ACCOUNT WHERE PID = #{pid}
    </select>
</mapper>